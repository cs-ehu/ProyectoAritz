\hypertarget{_log_in___register_8java_source}{}\section{Log\+In\+\_\+\+Register.\+java}
\label{_log_in___register_8java_source}\index{git/proyecto-\/jacteam-\/1/JACteam/src/main/java/pack/LogIn\_Register.java@{git/proyecto-\/jacteam-\/1/JACteam/src/main/java/pack/LogIn\_Register.java}}

\begin{DoxyCode}{0}
\DoxyCodeLine{00001 \textcolor{keyword}{package }pack;}
\DoxyCodeLine{00002 }
\DoxyCodeLine{00003 \textcolor{keyword}{import} java.awt.BorderLayout;}
\DoxyCodeLine{00004 \textcolor{keyword}{import} java.awt.Dimension;}
\DoxyCodeLine{00005 \textcolor{keyword}{import} java.awt.EventQueue;}
\DoxyCodeLine{00006 \textcolor{keyword}{import} java.awt.GridLayout;}
\DoxyCodeLine{00007 \textcolor{keyword}{import} java.awt.Toolkit;}
\DoxyCodeLine{00008 \textcolor{keyword}{import} java.awt.event.ActionEvent;}
\DoxyCodeLine{00009 \textcolor{keyword}{import} java.awt.event.ActionListener;}
\DoxyCodeLine{00010 \textcolor{keyword}{import} java.awt.event.WindowAdapter;}
\DoxyCodeLine{00011 }
\DoxyCodeLine{00012 \textcolor{keyword}{import} javax.swing.JButton;}
\DoxyCodeLine{00013 \textcolor{keyword}{import} javax.swing.JDialog;}
\DoxyCodeLine{00014 \textcolor{keyword}{import} javax.swing.JLabel;}
\DoxyCodeLine{00015 \textcolor{keyword}{import} javax.swing.JOptionPane;}
\DoxyCodeLine{00016 \textcolor{keyword}{import} javax.swing.JPanel;}
\DoxyCodeLine{00017 \textcolor{keyword}{import} javax.swing.JPasswordField;}
\DoxyCodeLine{00018 \textcolor{keyword}{import} javax.swing.JTextField;}
\DoxyCodeLine{00019 \textcolor{keyword}{import} javax.swing.table.TableModel;}
\DoxyCodeLine{00020 }
\DoxyCodeLine{00021 \textcolor{keyword}{import} pack.LogIn\_Register;}
\DoxyCodeLine{00022 }
\DoxyCodeLine{\Hypertarget{_log_in___register_8java_source_l00023}\mbox{\hyperlink{classpack_1_1_log_in___register}{00023}} \textcolor{keyword}{public} \textcolor{keyword}{class }\mbox{\hyperlink{classpack_1_1_log_in___register}{LogIn\_Register}} \textcolor{keyword}{extends} JDialog \{}
\DoxyCodeLine{00024 }
\DoxyCodeLine{00028     \textcolor{keyword}{private} JPanel panel;}
\DoxyCodeLine{00029     \textcolor{keyword}{private} JLabel lblNombre;}
\DoxyCodeLine{00030     \textcolor{keyword}{private} JTextField textField;}
\DoxyCodeLine{00031     \textcolor{keyword}{private} JLabel lblContraseña private JPasswordField passwordField;
    private JButton btnAceptar;
    private JButton btnBorrar;
    private JPanel panelBotones;
    private TableModel modeloDatosTabla;
    private Controlador mControlador;
    private static LogIn\_Register dialog;

    /**
     * Launch the application.
     */
    public static void main(String[] args) \{
        EventQueue.invokeLater(new Runnable() \{
            public void run() \{
                try \{
                    dialog = new LogIn\_Register();
                    dialog.setDefaultCloseOperation(JDialog.DISPOSE\_ON\_CLOSE);
                    dialog.setVisible(true);
                \} catch (Exception e) \{
                    e.printStackTrace();
                \}
            \}
        \});

    \}

    /**
     * Create the dialog.
     */
    public LogIn\_Register() \{
        Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
        Dimension windowSize = this.getSize();
        if (windowSize.height > screenSize.height) \{
            windowSize.height = screenSize.height;
        \}
        if (windowSize.width > screenSize.width) \{
            windowSize.width = screenSize.width;
        \}
        setLocation((screenSize.width - windowSize.width) / 2, (screenSize.height - windowSize.height) / 2);
        initialize();
    \}

    private void initialize() \{
        setTitle("Bienvenid@ a BarBestial");
        setBounds(100, 100, 450, 300);
        getContentPane().add(getPanel(), BorderLayout.CENTER);
        getContentPane().add(getPanelBotones(), BorderLayout.SOUTH);
    \}

    private JPanel getPanel() \{
        if (panel == null) \{
            panel = new JPanel();
            panel.setLayout(new GridLayout(2, 2, 0, 0));
            panel.add(getLblNombre());
            panel.add(getTextField());
            panel.add(getLblContraseña());
            panel.add(getPasswordField());
        \}
        return panel;
    \}

    private JLabel getLblNombre() \{
        if (lblNombre == null) \{
            lblNombre = new JLabel("Nombre:");
        \}
        return lblNombre;
    \}

    private JTextField getTextField() \{
        if (textField == null) \{
            textField = new JTextField();
            textField.setColumns(10);
        \}
        return textField;
    \}

    private JLabel getLblContraseña() \{
        if (lblContraseña == null) \{
            lblContraseña = new JLabel("Contrase\(\backslash\)u00F1a:");
        \}
        return lblContraseña;
 \}

    private JPasswordField getPasswordField() \{
        if (passwordField == null) \{
            passwordField = new JPasswordField();
        \}
        return passwordField;
    \}

    private JButton getBtnAceptar() \{
        if (btnAceptar == null) \{
            btnAceptar = new JButton("Iniciar sesion");
            btnAceptar.setActionCommand("iniciar sesion");
            btnAceptar.addActionListener(getControlador());
        \}
        return btnAceptar;
    \}

    private JButton getBtnBorrar() \{
        if (btnBorrar == null) \{
            btnBorrar = new JButton("Registrar");
            btnBorrar.setActionCommand("Registrar");
            btnBorrar.addActionListener(getControlador());
        \}
        return btnBorrar;
    \}

    private JPanel getPanelBotones() \{
        if (panelBotones == null) \{
            panelBotones = new JPanel();
            panelBotones.add(getBtnAceptar());
            panelBotones.add(getBtnBorrar());
        \}
        return panelBotones;
    \}

    private TableModel getModeloDatosTabla() \{
        if (modeloDatosTabla == null) \{
            modeloDatosTabla = new BDModeloDatosTabla();
        \}
        return modeloDatosTabla;
    \}

    private Controlador getControlador() \{
        if (mControlador == null) \{
            mControlador = new Controlador();
        \}
        return mControlador;
    \}

    public class Controlador extends WindowAdapter implements ActionListener \{

        @Override
        public void actionPerformed(ActionEvent e) \{
            // TODO Auto-generated method stub
            if (e.getActionCommand().equalsIgnoreCase("Registrar")) \{
                BDInfoUsuario nuevoUsu = new BDInfoUsuario();
                // nuevoUsu.addDato(getTextField().getText());
                // nuevoUsu.addDato(new String(getPasswordField().getPassword()));
                // ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);
                BDbarbestialDAO.getbarbestialDAO().resgistrar(getTextField().getText(),
                        new String(getPasswordField().getPassword()));
                JOptionPane.showMessageDialog(LogIn\_Register.this, "A continuación inicie sesión para empezar a jugar",
                        "Usuario registrado", JOptionPane.PLAIN\_MESSAGE);
            \} else if (e.getActionCommand().equalsIgnoreCase("iniciar sesion")) \{
                if (!BDbarbestialDAO.getbarbestialDAO().existeUsuario(getTextField().getText())) \{
                    JOptionPane.showMessageDialog(LogIn\_Register.this,
                            "El nombre de usuario introducido aún no está registrado", "Cuidado",
                            JOptionPane.ERROR\_MESSAGE);
                \} else \{
                    if (!BDbarbestialDAO.getbarbestialDAO().contraseñaCorrecta(getTextField().getText(),
                            new String(getPasswordField().getPassword()))) \{
                        JOptionPane.showMessageDialog(LogIn\_Register.this, "La contraseña es incorrecta", "Cuidado",
                                JOptionPane.ERROR\_MESSAGE);

                    \} else \{
                        Partida.getPartida().setNombre(getTextField().getText());
                        Interfaz1.main(null);
                        dialog.setVisible(false);
                    \}
                \}
            \}
        \}

    \}
\}
;}
\DoxyCodeLine{00032     \textcolor{keyword}{private} JPasswordField passwordField;}
\DoxyCodeLine{00033     \textcolor{keyword}{private} JButton btnAceptar;}
\DoxyCodeLine{00034     \textcolor{keyword}{private} JButton btnBorrar;}
\DoxyCodeLine{00035     \textcolor{keyword}{private} JPanel panelBotones;}
\DoxyCodeLine{00036     \textcolor{keyword}{private} TableModel modeloDatosTabla;}
\DoxyCodeLine{00037     \textcolor{keyword}{private} \mbox{\hyperlink{classpack_1_1_log_in___register_1_1_controlador}{Controlador}} mControlador;}
\DoxyCodeLine{00038     \textcolor{keyword}{private} \textcolor{keyword}{static} \mbox{\hyperlink{classpack_1_1_log_in___register}{LogIn\_Register}} dialog;}
\DoxyCodeLine{00039 }
\DoxyCodeLine{\Hypertarget{_log_in___register_8java_source_l00043}\mbox{\hyperlink{classpack_1_1_log_in___register_ae63739c76a49164c4ff4e7bc0caec8d4}{00043}}     \textcolor{keyword}{public} \textcolor{keyword}{static} \textcolor{keywordtype}{void} \mbox{\hyperlink{classpack_1_1_log_in___register_ae63739c76a49164c4ff4e7bc0caec8d4}{main}}(String[] args) \{}
\DoxyCodeLine{00044         EventQueue.invokeLater(\textcolor{keyword}{new} Runnable() \{}
\DoxyCodeLine{00045             \textcolor{keyword}{public} \textcolor{keywordtype}{void} run() \{}
\DoxyCodeLine{00046                 \textcolor{keywordflow}{try} \{}
\DoxyCodeLine{00047                     dialog = \textcolor{keyword}{new} \mbox{\hyperlink{classpack_1_1_log_in___register_a70dc784075ce952609f2bd5b6ef924dc}{LogIn\_Register}}();}
\DoxyCodeLine{00048                     dialog.setDefaultCloseOperation(JDialog.DISPOSE\_ON\_CLOSE);}
\DoxyCodeLine{00049                     dialog.setVisible(\textcolor{keyword}{true});}
\DoxyCodeLine{00050                 \} \textcolor{keywordflow}{catch} (Exception e) \{}
\DoxyCodeLine{00051                     e.printStackTrace();}
\DoxyCodeLine{00052                 \}}
\DoxyCodeLine{00053             \}}
\DoxyCodeLine{00054         \});}
\DoxyCodeLine{00055 }
\DoxyCodeLine{00056     \}}
\DoxyCodeLine{00057 }
\DoxyCodeLine{\Hypertarget{_log_in___register_8java_source_l00061}\mbox{\hyperlink{classpack_1_1_log_in___register_a70dc784075ce952609f2bd5b6ef924dc}{00061}}     \textcolor{keyword}{public} \mbox{\hyperlink{classpack_1_1_log_in___register_a70dc784075ce952609f2bd5b6ef924dc}{LogIn\_Register}}() \{}
\DoxyCodeLine{00062         Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();}
\DoxyCodeLine{00063         Dimension windowSize = this.getSize();}
\DoxyCodeLine{00064         \textcolor{keywordflow}{if} (windowSize.height > screenSize.height) \{}
\DoxyCodeLine{00065             windowSize.height = screenSize.height;}
\DoxyCodeLine{00066         \}}
\DoxyCodeLine{00067         \textcolor{keywordflow}{if} (windowSize.width > screenSize.width) \{}
\DoxyCodeLine{00068             windowSize.width = screenSize.width;}
\DoxyCodeLine{00069         \}}
\DoxyCodeLine{00070         setLocation((screenSize.width - windowSize.width) / 2, (screenSize.height - windowSize.height) / 2);}
\DoxyCodeLine{00071         initialize();}
\DoxyCodeLine{00072     \}}
\DoxyCodeLine{00073 }
\DoxyCodeLine{00074     \textcolor{keyword}{private} \textcolor{keywordtype}{void} initialize() \{}
\DoxyCodeLine{00075         setTitle(\textcolor{stringliteral}{"Bienvenid@ a BarBestial"});}
\DoxyCodeLine{00076         setBounds(100, 100, 450, 300);}
\DoxyCodeLine{00077         getContentPane().add(getPanel(), BorderLayout.CENTER);}
\DoxyCodeLine{00078         getContentPane().add(getPanelBotones(), BorderLayout.SOUTH);}
\DoxyCodeLine{00079     \}}
\DoxyCodeLine{00080 }
\DoxyCodeLine{00081     \textcolor{keyword}{private} JPanel getPanel() \{}
\DoxyCodeLine{00082         \textcolor{keywordflow}{if} (panel == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00083             panel = \textcolor{keyword}{new} JPanel();}
\DoxyCodeLine{00084             panel.setLayout(\textcolor{keyword}{new} GridLayout(2, 2, 0, 0));}
\DoxyCodeLine{00085             panel.add(getLblNombre());}
\DoxyCodeLine{00086             panel.add(getTextField());}
\DoxyCodeLine{00087             panel.add(getLblContraseña
            panel.add(getPasswordField());
        \}
        return panel;
    \}

    private JLabel getLblNombre() \{
        if (lblNombre == null) \{
            lblNombre = new JLabel("Nombre:");
        \}
        return lblNombre;
    \}

    private JTextField getTextField() \{
        if (textField == null) \{
            textField = new JTextField();
            textField.setColumns(10);
        \}
        return textField;
    \}

    private JLabel getLblContraseña() \{
        if (lblContraseña == null) \{
            lblContraseña = new JLabel("Contrase\(\backslash\)u00F1a:");
        \}
        return lblContraseña;
 \}

    private JPasswordField getPasswordField() \{
        if (passwordField == null) \{
            passwordField = new JPasswordField();
        \}
        return passwordField;
    \}

    private JButton getBtnAceptar() \{
        if (btnAceptar == null) \{
            btnAceptar = new JButton("Iniciar sesion");
            btnAceptar.setActionCommand("iniciar sesion");
            btnAceptar.addActionListener(getControlador());
        \}
        return btnAceptar;
    \}

    private JButton getBtnBorrar() \{
        if (btnBorrar == null) \{
            btnBorrar = new JButton("Registrar");
            btnBorrar.setActionCommand("Registrar");
            btnBorrar.addActionListener(getControlador());
        \}
        return btnBorrar;
    \}

    private JPanel getPanelBotones() \{
        if (panelBotones == null) \{
            panelBotones = new JPanel();
            panelBotones.add(getBtnAceptar());
            panelBotones.add(getBtnBorrar());
        \}
        return panelBotones;
    \}

    private TableModel getModeloDatosTabla() \{
        if (modeloDatosTabla == null) \{
            modeloDatosTabla = new BDModeloDatosTabla();
        \}
        return modeloDatosTabla;
    \}

    private Controlador getControlador() \{
        if (mControlador == null) \{
            mControlador = new Controlador();
        \}
        return mControlador;
    \}

    public class Controlador extends WindowAdapter implements ActionListener \{

        @Override
        public void actionPerformed(ActionEvent e) \{
            // TODO Auto-generated method stub
            if (e.getActionCommand().equalsIgnoreCase("Registrar")) \{
                BDInfoUsuario nuevoUsu = new BDInfoUsuario();
                // nuevoUsu.addDato(getTextField().getText());
                // nuevoUsu.addDato(new String(getPasswordField().getPassword()));
                // ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);
                BDbarbestialDAO.getbarbestialDAO().resgistrar(getTextField().getText(),
                        new String(getPasswordField().getPassword()));
                JOptionPane.showMessageDialog(LogIn\_Register.this, "A continuación inicie sesión para empezar a jugar",
                        "Usuario registrado", JOptionPane.PLAIN\_MESSAGE);
            \} else if (e.getActionCommand().equalsIgnoreCase("iniciar sesion")) \{
                if (!BDbarbestialDAO.getbarbestialDAO().existeUsuario(getTextField().getText())) \{
                    JOptionPane.showMessageDialog(LogIn\_Register.this,
                            "El nombre de usuario introducido aún no está registrado", "Cuidado",
                            JOptionPane.ERROR\_MESSAGE);
                \} else \{
                    if (!BDbarbestialDAO.getbarbestialDAO().contraseñaCorrecta(getTextField().getText(),
                            new String(getPasswordField().getPassword()))) \{
                        JOptionPane.showMessageDialog(LogIn\_Register.this, "La contraseña es incorrecta", "Cuidado",
                                JOptionPane.ERROR\_MESSAGE);

                    \} else \{
                        Partida.getPartida().setNombre(getTextField().getText());
                        Interfaz1.main(null);
                        dialog.setVisible(false);
                    \}
                \}
            \}
        \}

    \}
\}
());}
\DoxyCodeLine{00088             panel.add(getPasswordField());}
\DoxyCodeLine{00089         \}}
\DoxyCodeLine{00090         \textcolor{keywordflow}{return} panel;}
\DoxyCodeLine{00091     \}}
\DoxyCodeLine{00092 }
\DoxyCodeLine{00093     \textcolor{keyword}{private} JLabel getLblNombre() \{}
\DoxyCodeLine{00094         \textcolor{keywordflow}{if} (lblNombre == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00095             lblNombre = \textcolor{keyword}{new} JLabel(\textcolor{stringliteral}{"Nombre:"});}
\DoxyCodeLine{00096         \}}
\DoxyCodeLine{00097         \textcolor{keywordflow}{return} lblNombre;}
\DoxyCodeLine{00098     \}}
\DoxyCodeLine{00099 }
\DoxyCodeLine{00100     \textcolor{keyword}{private} JTextField getTextField() \{}
\DoxyCodeLine{00101         \textcolor{keywordflow}{if} (textField == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00102             textField = \textcolor{keyword}{new} JTextField();}
\DoxyCodeLine{00103             textField.setColumns(10);}
\DoxyCodeLine{00104         \}}
\DoxyCodeLine{00105         \textcolor{keywordflow}{return} textField;}
\DoxyCodeLine{00106     \}}
\DoxyCodeLine{00107 }
\DoxyCodeLine{00108     \textcolor{keyword}{private} JLabel getLblContraseña \{
        if (lblContraseña == null) \{
            lblContraseña = new JLabel("Contrase\(\backslash\)u00F1a:");
        \}
        return lblContraseña;
 \}

    private JPasswordField getPasswordField() \{
        if (passwordField == null) \{
            passwordField = new JPasswordField();
        \}
        return passwordField;
    \}

    private JButton getBtnAceptar() \{
        if (btnAceptar == null) \{
            btnAceptar = new JButton("Iniciar sesion");
            btnAceptar.setActionCommand("iniciar sesion");
            btnAceptar.addActionListener(getControlador());
        \}
        return btnAceptar;
    \}

    private JButton getBtnBorrar() \{
        if (btnBorrar == null) \{
            btnBorrar = new JButton("Registrar");
            btnBorrar.setActionCommand("Registrar");
            btnBorrar.addActionListener(getControlador());
        \}
        return btnBorrar;
    \}

    private JPanel getPanelBotones() \{
        if (panelBotones == null) \{
            panelBotones = new JPanel();
            panelBotones.add(getBtnAceptar());
            panelBotones.add(getBtnBorrar());
        \}
        return panelBotones;
    \}

    private TableModel getModeloDatosTabla() \{
        if (modeloDatosTabla == null) \{
            modeloDatosTabla = new BDModeloDatosTabla();
        \}
        return modeloDatosTabla;
    \}

    private Controlador getControlador() \{
        if (mControlador == null) \{
            mControlador = new Controlador();
        \}
        return mControlador;
    \}

    public class Controlador extends WindowAdapter implements ActionListener \{

        @Override
        public void actionPerformed(ActionEvent e) \{
            // TODO Auto-generated method stub
            if (e.getActionCommand().equalsIgnoreCase("Registrar")) \{
                BDInfoUsuario nuevoUsu = new BDInfoUsuario();
                // nuevoUsu.addDato(getTextField().getText());
                // nuevoUsu.addDato(new String(getPasswordField().getPassword()));
                // ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);
                BDbarbestialDAO.getbarbestialDAO().resgistrar(getTextField().getText(),
                        new String(getPasswordField().getPassword()));
                JOptionPane.showMessageDialog(LogIn\_Register.this, "A continuación inicie sesión para empezar a jugar",
                        "Usuario registrado", JOptionPane.PLAIN\_MESSAGE);
            \} else if (e.getActionCommand().equalsIgnoreCase("iniciar sesion")) \{
                if (!BDbarbestialDAO.getbarbestialDAO().existeUsuario(getTextField().getText())) \{
                    JOptionPane.showMessageDialog(LogIn\_Register.this,
                            "El nombre de usuario introducido aún no está registrado", "Cuidado",
                            JOptionPane.ERROR\_MESSAGE);
                \} else \{
                    if (!BDbarbestialDAO.getbarbestialDAO().contraseñaCorrecta(getTextField().getText(),
                            new String(getPasswordField().getPassword()))) \{
                        JOptionPane.showMessageDialog(LogIn\_Register.this, "La contraseña es incorrecta", "Cuidado",
                                JOptionPane.ERROR\_MESSAGE);

                    \} else \{
                        Partida.getPartida().setNombre(getTextField().getText());
                        Interfaz1.main(null);
                        dialog.setVisible(false);
                    \}
                \}
            \}
        \}

    \}
\}
() \{}
\DoxyCodeLine{00109         \textcolor{keywordflow}{if} (lblContraseña null) \{
            lblContraseña = new JLabel("Contrase\(\backslash\)u00F1a:");
        \}
        return lblContraseña;
 \}

    private JPasswordField getPasswordField() \{
        if (passwordField == null) \{
            passwordField = new JPasswordField();
        \}
        return passwordField;
    \}

    private JButton getBtnAceptar() \{
        if (btnAceptar == null) \{
            btnAceptar = new JButton("Iniciar sesion");
            btnAceptar.setActionCommand("iniciar sesion");
            btnAceptar.addActionListener(getControlador());
        \}
        return btnAceptar;
    \}

    private JButton getBtnBorrar() \{
        if (btnBorrar == null) \{
            btnBorrar = new JButton("Registrar");
            btnBorrar.setActionCommand("Registrar");
            btnBorrar.addActionListener(getControlador());
        \}
        return btnBorrar;
    \}

    private JPanel getPanelBotones() \{
        if (panelBotones == null) \{
            panelBotones = new JPanel();
            panelBotones.add(getBtnAceptar());
            panelBotones.add(getBtnBorrar());
        \}
        return panelBotones;
    \}

    private TableModel getModeloDatosTabla() \{
        if (modeloDatosTabla == null) \{
            modeloDatosTabla = new BDModeloDatosTabla();
        \}
        return modeloDatosTabla;
    \}

    private Controlador getControlador() \{
        if (mControlador == null) \{
            mControlador = new Controlador();
        \}
        return mControlador;
    \}

    public class Controlador extends WindowAdapter implements ActionListener \{

        @Override
        public void actionPerformed(ActionEvent e) \{
            // TODO Auto-generated method stub
            if (e.getActionCommand().equalsIgnoreCase("Registrar")) \{
                BDInfoUsuario nuevoUsu = new BDInfoUsuario();
                // nuevoUsu.addDato(getTextField().getText());
                // nuevoUsu.addDato(new String(getPasswordField().getPassword()));
                // ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);
                BDbarbestialDAO.getbarbestialDAO().resgistrar(getTextField().getText(),
                        new String(getPasswordField().getPassword()));
                JOptionPane.showMessageDialog(LogIn\_Register.this, "A continuación inicie sesión para empezar a jugar",
                        "Usuario registrado", JOptionPane.PLAIN\_MESSAGE);
            \} else if (e.getActionCommand().equalsIgnoreCase("iniciar sesion")) \{
                if (!BDbarbestialDAO.getbarbestialDAO().existeUsuario(getTextField().getText())) \{
                    JOptionPane.showMessageDialog(LogIn\_Register.this,
                            "El nombre de usuario introducido aún no está registrado", "Cuidado",
                            JOptionPane.ERROR\_MESSAGE);
                \} else \{
                    if (!BDbarbestialDAO.getbarbestialDAO().contraseñaCorrecta(getTextField().getText(),
                            new String(getPasswordField().getPassword()))) \{
                        JOptionPane.showMessageDialog(LogIn\_Register.this, "La contraseña es incorrecta", "Cuidado",
                                JOptionPane.ERROR\_MESSAGE);

                    \} else \{
                        Partida.getPartida().setNombre(getTextField().getText());
                        Interfaz1.main(null);
                        dialog.setVisible(false);
                    \}
                \}
            \}
        \}

    \}
\}
 == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00110             lblContraseña new JLabel("Contrase\(\backslash\)u00F1a:");
        \}
        return lblContraseña;
 \}

    private JPasswordField getPasswordField() \{
        if (passwordField == null) \{
            passwordField = new JPasswordField();
        \}
        return passwordField;
    \}

    private JButton getBtnAceptar() \{
        if (btnAceptar == null) \{
            btnAceptar = new JButton("Iniciar sesion");
            btnAceptar.setActionCommand("iniciar sesion");
            btnAceptar.addActionListener(getControlador());
        \}
        return btnAceptar;
    \}

    private JButton getBtnBorrar() \{
        if (btnBorrar == null) \{
            btnBorrar = new JButton("Registrar");
            btnBorrar.setActionCommand("Registrar");
            btnBorrar.addActionListener(getControlador());
        \}
        return btnBorrar;
    \}

    private JPanel getPanelBotones() \{
        if (panelBotones == null) \{
            panelBotones = new JPanel();
            panelBotones.add(getBtnAceptar());
            panelBotones.add(getBtnBorrar());
        \}
        return panelBotones;
    \}

    private TableModel getModeloDatosTabla() \{
        if (modeloDatosTabla == null) \{
            modeloDatosTabla = new BDModeloDatosTabla();
        \}
        return modeloDatosTabla;
    \}

    private Controlador getControlador() \{
        if (mControlador == null) \{
            mControlador = new Controlador();
        \}
        return mControlador;
    \}

    public class Controlador extends WindowAdapter implements ActionListener \{

        @Override
        public void actionPerformed(ActionEvent e) \{
            // TODO Auto-generated method stub
            if (e.getActionCommand().equalsIgnoreCase("Registrar")) \{
                BDInfoUsuario nuevoUsu = new BDInfoUsuario();
                // nuevoUsu.addDato(getTextField().getText());
                // nuevoUsu.addDato(new String(getPasswordField().getPassword()));
                // ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);
                BDbarbestialDAO.getbarbestialDAO().resgistrar(getTextField().getText(),
                        new String(getPasswordField().getPassword()));
                JOptionPane.showMessageDialog(LogIn\_Register.this, "A continuación inicie sesión para empezar a jugar",
                        "Usuario registrado", JOptionPane.PLAIN\_MESSAGE);
            \} else if (e.getActionCommand().equalsIgnoreCase("iniciar sesion")) \{
                if (!BDbarbestialDAO.getbarbestialDAO().existeUsuario(getTextField().getText())) \{
                    JOptionPane.showMessageDialog(LogIn\_Register.this,
                            "El nombre de usuario introducido aún no está registrado", "Cuidado",
                            JOptionPane.ERROR\_MESSAGE);
                \} else \{
                    if (!BDbarbestialDAO.getbarbestialDAO().contraseñaCorrecta(getTextField().getText(),
                            new String(getPasswordField().getPassword()))) \{
                        JOptionPane.showMessageDialog(LogIn\_Register.this, "La contraseña es incorrecta", "Cuidado",
                                JOptionPane.ERROR\_MESSAGE);

                    \} else \{
                        Partida.getPartida().setNombre(getTextField().getText());
                        Interfaz1.main(null);
                        dialog.setVisible(false);
                    \}
                \}
            \}
        \}

    \}
\}
 = \textcolor{keyword}{new} JLabel(\textcolor{stringliteral}{"Contrase\(\backslash\)u00F1a:"});}
\DoxyCodeLine{00111         \}}
\DoxyCodeLine{00112         \textcolor{keywordflow}{return} lblContraseña \}

    private JPasswordField getPasswordField() \{
        if (passwordField == null) \{
            passwordField = new JPasswordField();
        \}
        return passwordField;
    \}

    private JButton getBtnAceptar() \{
        if (btnAceptar == null) \{
            btnAceptar = new JButton("Iniciar sesion");
            btnAceptar.setActionCommand("iniciar sesion");
            btnAceptar.addActionListener(getControlador());
        \}
        return btnAceptar;
    \}

    private JButton getBtnBorrar() \{
        if (btnBorrar == null) \{
            btnBorrar = new JButton("Registrar");
            btnBorrar.setActionCommand("Registrar");
            btnBorrar.addActionListener(getControlador());
        \}
        return btnBorrar;
    \}

    private JPanel getPanelBotones() \{
        if (panelBotones == null) \{
            panelBotones = new JPanel();
            panelBotones.add(getBtnAceptar());
            panelBotones.add(getBtnBorrar());
        \}
        return panelBotones;
    \}

    private TableModel getModeloDatosTabla() \{
        if (modeloDatosTabla == null) \{
            modeloDatosTabla = new BDModeloDatosTabla();
        \}
        return modeloDatosTabla;
    \}

    private Controlador getControlador() \{
        if (mControlador == null) \{
            mControlador = new Controlador();
        \}
        return mControlador;
    \}

    public class Controlador extends WindowAdapter implements ActionListener \{

        @Override
        public void actionPerformed(ActionEvent e) \{
            // TODO Auto-generated method stub
            if (e.getActionCommand().equalsIgnoreCase("Registrar")) \{
                BDInfoUsuario nuevoUsu = new BDInfoUsuario();
                // nuevoUsu.addDato(getTextField().getText());
                // nuevoUsu.addDato(new String(getPasswordField().getPassword()));
                // ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);
                BDbarbestialDAO.getbarbestialDAO().resgistrar(getTextField().getText(),
                        new String(getPasswordField().getPassword()));
                JOptionPane.showMessageDialog(LogIn\_Register.this, "A continuación inicie sesión para empezar a jugar",
                        "Usuario registrado", JOptionPane.PLAIN\_MESSAGE);
            \} else if (e.getActionCommand().equalsIgnoreCase("iniciar sesion")) \{
                if (!BDbarbestialDAO.getbarbestialDAO().existeUsuario(getTextField().getText())) \{
                    JOptionPane.showMessageDialog(LogIn\_Register.this,
                            "El nombre de usuario introducido aún no está registrado", "Cuidado",
                            JOptionPane.ERROR\_MESSAGE);
                \} else \{
                    if (!BDbarbestialDAO.getbarbestialDAO().contraseñaCorrecta(getTextField().getText(),
                            new String(getPasswordField().getPassword()))) \{
                        JOptionPane.showMessageDialog(LogIn\_Register.this, "La contraseña es incorrecta", "Cuidado",
                                JOptionPane.ERROR\_MESSAGE);

                    \} else \{
                        Partida.getPartida().setNombre(getTextField().getText());
                        Interfaz1.main(null);
                        dialog.setVisible(false);
                    \}
                \}
            \}
        \}

    \}
\}
;}
\DoxyCodeLine{00113     \}}
\DoxyCodeLine{00114 }
\DoxyCodeLine{00115     \textcolor{keyword}{private} JPasswordField getPasswordField() \{}
\DoxyCodeLine{00116         \textcolor{keywordflow}{if} (passwordField == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00117             passwordField = \textcolor{keyword}{new} JPasswordField();}
\DoxyCodeLine{00118         \}}
\DoxyCodeLine{00119         \textcolor{keywordflow}{return} passwordField;}
\DoxyCodeLine{00120     \}}
\DoxyCodeLine{00121 }
\DoxyCodeLine{00122     \textcolor{keyword}{private} JButton getBtnAceptar() \{}
\DoxyCodeLine{00123         \textcolor{keywordflow}{if} (btnAceptar == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00124             btnAceptar = \textcolor{keyword}{new} JButton(\textcolor{stringliteral}{"Iniciar sesion"});}
\DoxyCodeLine{00125             btnAceptar.setActionCommand(\textcolor{stringliteral}{"iniciar sesion"});}
\DoxyCodeLine{00126             btnAceptar.addActionListener(getControlador());}
\DoxyCodeLine{00127         \}}
\DoxyCodeLine{00128         \textcolor{keywordflow}{return} btnAceptar;}
\DoxyCodeLine{00129     \}}
\DoxyCodeLine{00130 }
\DoxyCodeLine{00131     \textcolor{keyword}{private} JButton getBtnBorrar() \{}
\DoxyCodeLine{00132         \textcolor{keywordflow}{if} (btnBorrar == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00133             btnBorrar = \textcolor{keyword}{new} JButton(\textcolor{stringliteral}{"Registrar"});}
\DoxyCodeLine{00134             btnBorrar.setActionCommand(\textcolor{stringliteral}{"Registrar"});}
\DoxyCodeLine{00135             btnBorrar.addActionListener(getControlador());}
\DoxyCodeLine{00136         \}}
\DoxyCodeLine{00137         \textcolor{keywordflow}{return} btnBorrar;}
\DoxyCodeLine{00138     \}}
\DoxyCodeLine{00139 }
\DoxyCodeLine{00140     \textcolor{keyword}{private} JPanel getPanelBotones() \{}
\DoxyCodeLine{00141         \textcolor{keywordflow}{if} (panelBotones == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00142             panelBotones = \textcolor{keyword}{new} JPanel();}
\DoxyCodeLine{00143             panelBotones.add(getBtnAceptar());}
\DoxyCodeLine{00144             panelBotones.add(getBtnBorrar());}
\DoxyCodeLine{00145         \}}
\DoxyCodeLine{00146         \textcolor{keywordflow}{return} panelBotones;}
\DoxyCodeLine{00147     \}}
\DoxyCodeLine{00148 }
\DoxyCodeLine{00149     \textcolor{keyword}{private} TableModel getModeloDatosTabla() \{}
\DoxyCodeLine{00150         \textcolor{keywordflow}{if} (modeloDatosTabla == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00151             modeloDatosTabla = \textcolor{keyword}{new} BDModeloDatosTabla();}
\DoxyCodeLine{00152         \}}
\DoxyCodeLine{00153         \textcolor{keywordflow}{return} modeloDatosTabla;}
\DoxyCodeLine{00154     \}}
\DoxyCodeLine{00155 }
\DoxyCodeLine{00156     \textcolor{keyword}{private} Controlador getControlador() \{}
\DoxyCodeLine{00157         \textcolor{keywordflow}{if} (mControlador == \textcolor{keyword}{null}) \{}
\DoxyCodeLine{00158             mControlador = \textcolor{keyword}{new} Controlador();}
\DoxyCodeLine{00159         \}}
\DoxyCodeLine{00160         \textcolor{keywordflow}{return} mControlador;}
\DoxyCodeLine{00161     \}}
\DoxyCodeLine{00162 }
\DoxyCodeLine{\Hypertarget{_log_in___register_8java_source_l00163}\mbox{\hyperlink{classpack_1_1_log_in___register_1_1_controlador}{00163}}     \textcolor{keyword}{public} \textcolor{keyword}{class }\mbox{\hyperlink{classpack_1_1_log_in___register_1_1_controlador}{Controlador}} \textcolor{keyword}{extends} WindowAdapter implements ActionListener \{}
\DoxyCodeLine{00164 }
\DoxyCodeLine{00165         @Override}
\DoxyCodeLine{00166         \textcolor{keyword}{public} \textcolor{keywordtype}{void} actionPerformed(ActionEvent e) \{}
\DoxyCodeLine{00167             \textcolor{comment}{// TODO Auto-generated method stub}}
\DoxyCodeLine{00168             \textcolor{keywordflow}{if} (e.getActionCommand().equalsIgnoreCase(\textcolor{stringliteral}{"Registrar"})) \{}
\DoxyCodeLine{00169                 \mbox{\hyperlink{classpack_1_1_b_d_info_usuario}{BDInfoUsuario}} nuevoUsu = \textcolor{keyword}{new} \mbox{\hyperlink{classpack_1_1_b_d_info_usuario}{BDInfoUsuario}}();}
\DoxyCodeLine{00170                 \textcolor{comment}{// nuevoUsu.addDato(getTextField().getText());}}
\DoxyCodeLine{00171                 \textcolor{comment}{// nuevoUsu.addDato(new String(getPasswordField().getPassword()));}}
\DoxyCodeLine{00172                 \textcolor{comment}{// ((BDModeloDatosTabla) getModeloDatosTabla()).addUsuario(nuevoUsu);}}
\DoxyCodeLine{00173                 \mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o}{BDbarbestialDAO}}.\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o_a0ba1c678bf175e2ccd819f5d40f16b29}{getbarbestialDAO}}().\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o_a8f222e7567d413b57bb85e12ba98a9df}{resgistrar}}(getTextField().getText(),}
\DoxyCodeLine{00174                         \textcolor{keyword}{new} String(getPasswordField().getPassword()));}
\DoxyCodeLine{00175                 JOptionPane.showMessageDialog(\mbox{\hyperlink{classpack_1_1_log_in___register}{LogIn\_Register}}.this, \textcolor{stringliteral}{"A continuación inicie sesión para empezar a jugar"},}
\DoxyCodeLine{00176                         \textcolor{stringliteral}{"Usuario registrado"}, JOptionPane.PLAIN\_MESSAGE);}
\DoxyCodeLine{00177             \} \textcolor{keywordflow}{else} \textcolor{keywordflow}{if} (e.getActionCommand().equalsIgnoreCase(\textcolor{stringliteral}{"iniciar sesion"})) \{}
\DoxyCodeLine{00178                 \textcolor{keywordflow}{if} (!\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o}{BDbarbestialDAO}}.\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o_a0ba1c678bf175e2ccd819f5d40f16b29}{getbarbestialDAO}}().\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o_a48b1476b661d49d11face1827141e778}{existeUsuario}}(getTextField().getText())) \{}
\DoxyCodeLine{00179                     JOptionPane.showMessageDialog(\mbox{\hyperlink{classpack_1_1_log_in___register}{LogIn\_Register}}.this,}
\DoxyCodeLine{00180                             \textcolor{stringliteral}{"El nombre de usuario introducido aún no está registrado"}, \textcolor{stringliteral}{"Cuidado"},}
\DoxyCodeLine{00181                             JOptionPane.ERROR\_MESSAGE);}
\DoxyCodeLine{00182                 \} \textcolor{keywordflow}{else} \{}
\DoxyCodeLine{00183                     \textcolor{keywordflow}{if} (!\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o}{BDbarbestialDAO}}.\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o_a0ba1c678bf175e2ccd819f5d40f16b29}{getbarbestialDAO}}().\mbox{\hyperlink{classpack_1_1_b_dbarbestial_d_a_o_a71f43b2a052487a7d83de7fb889e7d93}{contraseñaCorrecta}}(getTextField().getText(),}
\DoxyCodeLine{00184                             \textcolor{keyword}{new} String(getPasswordField().getPassword()))) \{}
\DoxyCodeLine{00185                         JOptionPane.showMessageDialog(\mbox{\hyperlink{classpack_1_1_log_in___register}{LogIn\_Register}}.this, \textcolor{stringliteral}{"La contraseña es incorrecta"}, \textcolor{stringliteral}{"Cuidado"},}
\DoxyCodeLine{00186                                 JOptionPane.ERROR\_MESSAGE);}
\DoxyCodeLine{00187 }
\DoxyCodeLine{00188                     \} \textcolor{keywordflow}{else} \{}
\DoxyCodeLine{00189                         \mbox{\hyperlink{classpack_1_1_partida}{Partida}}.\mbox{\hyperlink{classpack_1_1_partida_a69c6b1f81c589ffc94bd10fbe25bddea}{getPartida}}().\mbox{\hyperlink{classpack_1_1_partida_af845a85b3b85b20eb7ffb50e0dc7add4}{setNombre}}(getTextField().getText());}
\DoxyCodeLine{00190                         \mbox{\hyperlink{classpack_1_1_interfaz1}{Interfaz1}}.\mbox{\hyperlink{classpack_1_1_interfaz1_aa6a2189460a4c501ae7deabb5fd658c8}{main}}(\textcolor{keyword}{null});}
\DoxyCodeLine{00191                         dialog.setVisible(\textcolor{keyword}{false});}
\DoxyCodeLine{00192                     \}}
\DoxyCodeLine{00193                 \}}
\DoxyCodeLine{00194             \}}
\DoxyCodeLine{00195         \}}
\DoxyCodeLine{00196 }
\DoxyCodeLine{00197     \}}
\DoxyCodeLine{00198 \}}

\end{DoxyCode}
